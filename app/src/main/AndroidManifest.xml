<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fornary4.exercise">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IdeologicalExercise">
        <activity android:name=".ResultActivity"></activity>
        <activity android:name=".ExamActivity" />
        <activity android:name=".ViewJudgeActivity" />
        <activity android:name=".ViewChoiceActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ErrorActivity" />
        <activity android:name=".MultipleChoiceActivity" />
        <activity android:name=".JudgeActivity" />
        <activity android:name=".SingleChoiceActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>